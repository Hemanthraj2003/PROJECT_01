/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  eslint: {
    ignoreDuringBuilds: true, // disables lint during next build
  },
};

module.exports = nextConfig;
